
p oh hi

#pgs
	#pg

script.
	loadjs.ready('dbind', function() {
		console.log('dbind')

		$('#pgs').pagination({
			dataSource: [1, 2, 3, 4, 5, 6, 7],
			callback: function(data, pagination) {

				var html = simpleTemplating(data)

				$('#pg').html(html)
				console.log(pagination)
			}//cb
		})


		function simpleTemplating(data) {
			var html = '<ul>';
			$.each(data, function(index, item){
				html += '<li>'+ item +'</li>';
			});
			html += '</ul>';
			return html;
		}

	})//ready




	function bind(eID, field, value) {

	}

	loadjs.ready('PRELoadB', function() {
		loadjs(['//cdn.jsdelivr.net/npm/moment@2.22.0/min/moment.min.js'
		,'//cdn.jsdelivr.net/npm/mustache@2.3.0/mustache.min.js'
		, 'https://cdn.jsdelivr.net/npm/paginationjs@2.1.2/dist/pagination.min.js'
		],'dbind')
	})